<Window x:Class="Directory_Scanner.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:model="clr-namespace:Directory_Scanner.UI.Model"
        xmlns:converter1="clr-namespace:Directory_Scanner.UI.Converter"
        mc:Ignorable="d"
        Title="Directory Scanner" Height="450" Width="800">
    <Window.DataContext>
        <model:MainWindowViewModel />
    </Window.DataContext>
    <Window.Resources>
        <converter1:FileTypeToImageConverter x:Key="FileTypeToImageConverter" />
        <converter1:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converter1:InverseBoolConverter x:Key="InverseBoolConverter" />
        <converter1:BoolToInverseVisibilityConverter x:Key="BoolToInverseVisibilityConverter" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <Button Content="Scan" 
                    Command="{Binding StartScanCommand}" 
                    Margin="5" 
                    Padding="10,2" 
                    Width="350"
                    IsEnabled="{Binding IsScanning, Converter={StaticResource InverseBoolConverter}}"/>
            <Button Content="Cancel" 
                    Command="{Binding CancelScanCommand}" 
                    Margin="5" 
                    Padding="10,2" 
                    Width="350"
                    IsEnabled="{Binding IsScanning}"/>
        </StackPanel>
        
        <TextBlock Grid.Row="1" 
                   Text="{Binding StatusMessage}" 
                   Margin="5"
                   FontWeight="Bold"
                   Visibility="{Binding IsScanning, Converter={StaticResource BoolToVisibilityConverter}}"/>
        
        <Border Grid.Row="1" 
                Background="LightYellow" 
                BorderBrush="Orange" 
                BorderThickness="1"
                Padding="5"
                Margin="5"
                Visibility="{Binding IsCancelled, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="{Binding StatusMessage}" 
                       FontWeight="Bold" 
                       Foreground="DarkOrange"/>
        </Border>
        
        <TreeView Grid.Row="2" 
                  ItemsSource="{Binding RootItems}" 
                  Margin="5"
                  VirtualizingStackPanel.IsVirtualizing="True"
                  VirtualizingStackPanel.VirtualizationMode="Recycling">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{Binding Type, Converter={StaticResource FileTypeToImageConverter}}"
                               Width="16" Height="16" Margin="0,0,4,0" />
                        <TextBlock Text="{Binding Name}" Margin="2"/>
                        <TextBlock Text="{Binding SizeText}" Margin="2"/>
                        <TextBlock Text="{Binding Percent, StringFormat={}{0:F2}%}" Margin="2"/>
                        <TextBlock Text="Loading..." 
                                   Margin="2"
                                   FontWeight="Bold"
                                   Foreground="Gray"
                                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</Window>